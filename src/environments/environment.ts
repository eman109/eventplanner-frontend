export const environment = {
  production: false,
  /**
   * Base URL of your backend API
   * Adjust this to match your server address
   * Example: if your backend runs on http://localhost:3000/api
   */
  apiBaseUrl: 'http://localhost:3000/api'
};